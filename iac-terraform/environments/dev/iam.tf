# Development Environment - IAM Configuration
# Additional IAM resources specific to the development environment

# GitHub OIDC Provider for CI/CD
resource "aws_iam_openid_connect_provider" "github" {
  count = var.github_oidc_provider_arn == null ? 1 : 0

  url = "https://token.actions.githubusercontent.com"

  client_id_list = [
    "sts.amazonaws.com"
  ]

  thumbprint_list = [
    "6938fd4d98bab03faadb97b34396831e3780aea1",
    "1c58a3a8518e8759bf075b76b750d4f2df264fcd"
  ]

  tags = merge(var.global_config.common_tags, {
    Name        = "github-oidc-provider"
    Environment = "dev"
  })
}

# Output the OIDC provider ARN for use in other modules
locals {
  github_oidc_provider_arn = var.github_oidc_provider_arn != null ? var.github_oidc_provider_arn : aws_iam_openid_connect_provider.github[0].arn
} 